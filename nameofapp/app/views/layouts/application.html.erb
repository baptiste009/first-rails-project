<!DOCTYPE html>
<html>
<head>
  <title>Nameofapp</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>

  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
</head>
<body>
	<nav class="navbar navbar-inverse navbar-static-top" role="navigation"><!--navbar starts here-->
		<div class="container-fluid">
			<div class="navbar-header">
		     	<button type="button" class="navbar-toggle" data-toggle="collapse" 
		      	data-target=".navbar-collapse">
		     	<span class="sr-only">Toggle navigation</span>
		     	<span class="icon-bar"></span>
		     	<span class="icon-bar"></span>
		     	<span class="icon-bar"></span>
		     	</button>
		    	<%= link_to "Powerpay", root_path, class: "navbar-brand"%>
	   		</div>
			<div class="navbar-collapse collapse">
	    		<ul class="nav navbar-nav">
					<li><%= link_to "Create new product", new_product_path %></li>
					<li><%= link_to "Products", products_path %></li>
	    		</ul>
	    		<ul class="nav pull-right">
	    			<li>
	    				<% if user_signed_in? %>
	    					<%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
    					<% else %>
    						<%= link_to('Login', new_user_session_path)%>
						<% end %>
	    			</li>
	    		</ul>
				<%= form_tag("/products", method: "get", :class => "navbar-form navbar-left") do %>
  					<%= label_tag(:q, "Search for:", :class => "form-control") %>
  					<%= text_field_tag(:q, nil, :class => "form-control") %>
					<%= submit_tag("Search", :class => "btn btn-default navbar-btn") %>
				<% end %>
	    	</div>
	  	</div>
	</nav>
	<% if notice %>
		<div class="alert alert-info" role="alert">
			<p class="notice"><%= notice %></p>
		</div>
	<% end %>
	<% if alert %>
		<div class="alert alert-danger" role="alert">
	    	<p class="alert"><%= alert %></p>
	    </div>
    <% end %>
	<h1>Welcome to Powerpay</h1>
	<h4>Your source for innovative payment solutions</h4>

	<%= yield %>

	<footer class="footer">
  		<div class="container footer">
    		<p class="text-muted">Powerpay 2015</p>
  		</div>
	</footer>
</body>

</html>
